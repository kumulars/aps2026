server {
      listen 80;
      server_name hoopai.org www.hoopai.org 159.203.115.118;

      client_max_body_size 100M;

      location / {
          proxy_pass http://127.0.0.1:8000;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Forwarded-Proto $scheme;
      }

      location /static/ {
          alias /var/www/aps2026/static/;
      }

      location /media/ {
          alias /var/www/aps2026/media/;
      }
  }


  Useful Commands for Future:

  - Check status: sudo systemctl status aps2026
  - Restart Django: sudo systemctl restart aps2026
  - View logs: sudo journalctl -u aps2026 -f
  - Stop service: sudo systemctl stop aps2026